#include <stdio.h>

void main () {
    
    char print='*';
    int row,col;
    int noOfRows=5;
    
    for(row = 1; row <= noOfRows; row++) {
        
        for(col = 1; col <= rows; col++) {
            
        printf("%c", print);
        
        }
        printf("\n");
    }
    
}